# MULADD

DEPRECATED: BEL of the legacy FABulous project template, kept for backward compatibility. Do not use it in new tiles.

Used by the `fabulous` library tiles `DSP_bot`. Verilog and VHDL sources sit side by side; the tile CSVs select one through the `{HDL_SUFFIX}` placeholder.
