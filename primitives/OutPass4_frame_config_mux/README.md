# OutPass4_frame_config_mux

DEPRECATED: BEL of the legacy FABulous project template, kept for backward compatibility. Do not use it in new tiles.

Used by the `fabulous` library tiles `RAM_IO`. Verilog and VHDL sources sit side by side; the tile CSVs select one through the `{HDL_SUFFIX}` placeholder.
